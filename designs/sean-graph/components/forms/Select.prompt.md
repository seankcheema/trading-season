Custom dropdown with a floating listbox; use when the list needs styling a native select can't give (filters in toolbars).

```jsx
<Select placeholder="Order status" options={['FILLED', 'PENDING', 'REJECTED']} value={s} onChange={setS} width={180} />
```
