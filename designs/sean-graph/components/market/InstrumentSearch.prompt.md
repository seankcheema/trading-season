Search box that finds instruments by symbol or name and opens a trade.

```jsx
<InstrumentSearch instruments={INSTRUMENTS} onSelect={(i) => openOrder(i)} />
```

Empty result copy: `No instruments match "xyz"`.
