Compact pill segment for chart ranges; `SideToggle` is the larger buy/sell switch on the order ticket.

```jsx
<TimeframeToggle value={tf} onChange={setTf} />
<SideToggle value={side} onChange={setSide} />
```

Active timeframe = cyan fill. Active sell = loss red with white text.
