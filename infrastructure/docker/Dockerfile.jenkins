FROM jenkins/jenkins:lts-jdk21

# Run as root to install dependencies
USER root

# Update package manager and install Node.js 20.x
RUN apt-get update && \
    apt-get install -y curl gnupg && \
    curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && \
    apt-get install -y nodejs && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/* /var/cache/apt/*

# Verify installations
RUN node --version && npm --version && java -version

# Switch back to jenkins user for security
USER jenkins

# Optional: Install additional Jenkins plugins for testing/coverage
# Uncomment if needed later
# COPY --chown=jenkins:jenkins plugins.txt /usr/share/jenkins/ref/plugins.txt
# RUN jenkins-plugin-cli -f /usr/share/jenkins/ref/plugins.txt
