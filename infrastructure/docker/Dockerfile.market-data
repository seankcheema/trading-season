FROM python:3.12-slim
WORKDIR /workspace
RUN apt-get update && apt-get install -y --no-install-recommends time && rm -rf /var/lib/apt/lists/*
COPY apps/market-data/db/scripts/requirements-ci.txt /tmp/requirements-ci.txt
RUN pip install --no-cache-dir -r /tmp/requirements-ci.txt
COPY apps/market-data/db /workspace/apps/market-data/db
ENTRYPOINT ["python"]
